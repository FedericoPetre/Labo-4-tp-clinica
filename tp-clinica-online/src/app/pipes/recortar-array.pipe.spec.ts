import { RecortarArrayPipe } from './recortar-array.pipe';

describe('RecortarArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RecortarArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
