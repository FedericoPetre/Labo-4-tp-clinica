import { OrdenFechaPipe } from './orden-fecha.pipe';

describe('OrdenFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
